class Solution {
    public int solution(String message) {
        StringBuilder sb = new StringBuilder(message);
        int len = sb.length();
        int answer = 2 * len;
        return answer;
    }
}